#!/usr/bin/env python


'''
Created on Nov 3, 2014

@author: Brady Johnson
'''


import sys
# This is a temporary solution until I get this application installed correctly
# Since the script has to be launched with sudo, it loses then env, namely PYTHONPATH
# so it cant find from where to import Flows.FlowEntries
# When FlowDebugger/Flows (and others) is correctly installed, this wont be a problem anymore.
sys.path.append('/home/mininet/workspace/FlowDebugger')
sys.path.append('/home/mcl/workspace/FlowDebugger')
#print sys.path

from Flows.FlowDebuggerMain import FlowDebuggerMain

if __name__ == '__main__':
    FlowDebuggerMain().main()
